import { BookLayout } from './BookLayout';

/**
 * App component
 * BookLayout manages page navigation internally using window.history API
 */
function App() {
  return <BookLayout />;
}

export default App;

